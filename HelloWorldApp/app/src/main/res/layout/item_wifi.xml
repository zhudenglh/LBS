<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="16dp"
    android:gravity="center_vertical"
    android:background="@android:color/white">

    <!-- WiFi 图标 -->
    <TextView
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:text="📶"
        android:textSize="24sp"
        android:gravity="center" />

    <!-- WiFi 信息 -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/wifiName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="ChinaNet-sqvk"
                android:textSize="15sp"
                android:textStyle="bold"
                android:textColor="#333333"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/wifiTag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="官方"
                android:textSize="11sp"
                android:textColor="#FFFFFF"
                android:background="@drawable/tag_official"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:layout_marginStart="8dp"
                android:visibility="gone" />
        </LinearLayout>

        <TextView
            android:id="@+id/wifiNote"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="可能需要认证"
            android:textSize="12sp"
            android:textColor="#999999"
            android:layout_marginTop="4dp"
            android:visibility="gone" />
    </LinearLayout>

    <!-- 连接按钮 -->
    <Button
        android:id="@+id/btnConnectWifi"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:text="免费直连"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:background="@drawable/button_wifi_connect"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:layout_marginStart="12dp"
        android:minWidth="0dp"
        android:minHeight="0dp" />

    <!-- 锁定图标（加密WiFi） -->
    <TextView
        android:id="@+id/wifiLockIcon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:text="🔒"
        android:textSize="20sp"
        android:gravity="center"
        android:layout_marginStart="12dp"
        android:visibility="gone" />

</LinearLayout>
